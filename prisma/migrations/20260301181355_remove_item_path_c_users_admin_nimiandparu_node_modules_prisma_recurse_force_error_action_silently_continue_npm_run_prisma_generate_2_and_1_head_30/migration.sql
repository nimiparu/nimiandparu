-- AlterTable
ALTER TABLE "Order" ADD COLUMN "budget" TEXT;
ALTER TABLE "Order" ADD COLUMN "clientName" TEXT;
ALTER TABLE "Order" ADD COLUMN "occasion" TEXT;

-- CreateTable
CREATE TABLE "OrderArtist" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "orderId" TEXT NOT NULL,
    "artistId" TEXT NOT NULL,
    CONSTRAINT "OrderArtist_orderId_fkey" FOREIGN KEY ("orderId") REFERENCES "Order" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT "OrderArtist_artistId_fkey" FOREIGN KEY ("artistId") REFERENCES "Artist" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);

-- CreateTable
CREATE TABLE "Artist" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "name" TEXT NOT NULL,
    "instagramId" TEXT NOT NULL,
    "signedUpAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateIndex
CREATE UNIQUE INDEX "OrderArtist_orderId_artistId_key" ON "OrderArtist"("orderId", "artistId");

-- CreateIndex
CREATE UNIQUE INDEX "Artist_instagramId_key" ON "Artist"("instagramId");
